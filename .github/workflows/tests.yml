name: Run Tests Automatically

on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    branches: [ master, main, develop ]

jobs:
  # test job disabled - will be re-enabled after UC-007 stabilization
  # test:
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       python-version: ['3.14']
  #
  #   steps:
  #   - uses: actions/checkout@v3
  #     with:
  #       fetch-depth: 0
  #
  #   - name: Set up Python ${{ matrix.python-version }}
  #     uses: actions/setup-python@v4
  #     with:
  #       python-version: ${{ matrix.python-version }}
  #
  #   - name: Install dependencies
  #     run: |
  #       python -m pip install --upgrade pip
  #       pip install -r requirements.txt
  #       pip install pytest pytest-cov pytest-sugar
  #
  #   - name: Run unit tests
  #     run: |
  #       pytest tests/unit/ -v --cov=src --cov-report=xml --cov-report=html
  #
  #   - name: Upload coverage reports
  #     uses: codecov/codecov-action@v3
  #     if: always()
  #     with:
  #       files: ./coverage.xml
  #       flags: unittests
  #       name: codecov-umbrella
  #
  #   - name: Comment PR with test results
  #     if: github.event_name == 'pull_request'
  #     uses: actions/github-script@v6
  #     with:
  #       script: |
  #         const fs = require('fs');
  #         const coverage = fs.existsSync('coverage.xml') ? 'Coverage report generated' : 'No coverage';
  #         github.rest.issues.createComment({
  #           issue_number: context.issue.number,
  #           owner: context.repo.owner,
  #           repo: context.repo.repo,
  #           body: '✅ Testes executados automaticamente!\n\n' + coverage
  #         })
  
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI/CD jobs temporarily disabled"
